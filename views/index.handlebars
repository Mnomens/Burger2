<h1>Burgers</h1>

<h2>Burgers on the menu</h2>

<ul>
  {{#each burgers2}}
    {{#unless eaten}}
      {{> burgers2/burger-block eaten=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been tried</h2>

<ul>
  {{#each burgers2}}
    {{#if eaten}}
      {{> burgers2/burger-block eaten=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten!<br>
    <input type="radio" name="eaten" value="0"> Not Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
